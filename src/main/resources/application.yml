spring:
  application:
    name: karaplan
  http:
    encoding:
      charset: UTF-8
      force: true
  jackson:
    default-property-inclusion: non-empty
  jpa:
    generateDdl: true
  security:
    user:
      name: root
      password: changeit
      roles: USER,ADMIN
server:
  port: 8080
  compression:
    enabled: true
